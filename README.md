# CI/CD pipeline security scanner

### A tool that runs in CI (as a GitHub Action, GitLab CI job, or standalone CLI) that scans commits/PRs for three things: hardcoded secrets, vulnerable dependencies, and insecure code patterns — then fails the build or posts a report if issues are found.